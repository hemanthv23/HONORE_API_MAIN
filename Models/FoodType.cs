namespace HONORE_API_MAIN.Models
{
    public enum FoodType
    {
        Veg = 0,    // For products that are strictly vegetarian (green indicator)
        SemiVeg = 1 // For products that are vegetarian but contain egg/dairy (yellow indicator)
    }
}